# These generated artifacts have recorded byte-level SHA256 values.
# Preserve their exact bytes across Windows/Linux checkouts and GitHub downloads.
reports/M2/figure_manifest.csv -text whitespace=trailing-space,space-before-tab,cr-at-eol
reports/M2/runs/** -text whitespace=trailing-space,space-before-tab,cr-at-eol
reports/M2/cohort_fix/** -text whitespace=trailing-space,space-before-tab,cr-at-eol
