# 数据预处理

主责：B；复核：C/D。

下一阶段实现：

1. 从recognition JSON抽取序列与元数据；
2. 核对完全重复合并关系；
3. 标记partial、非标准残基、来源缺失和Gvp类型冲突；
4. 用MMseqs2/CD-HIT建立同源簇；
5. 冻结discovery/validation和敏感性队列；
6. 输出数据版本、输入/输出哈希、排除原因和统计报告。

原始文件只读且不提交Git。处理脚本不得把未知值补成负例，也不得在删样本后仍沿用2078作为“clean”规模。PR必须包含实际复现命令和无簇泄漏检查。
