# GvpA 潜在功能区域识别与关键位点证据整合

五人机器学习课程项目，公共仓库为 [Lithium9767/MLP](https://github.com/Lithium9767/MLP)。当前主线不再给天然 GvpA 强行构造功能正负标签，而是利用蛋白语言模型分析无标签序列中的局部模式，再用进化、实验突变和结构证据验证候选区域。

> M1 事实核查：官方数据库中 **PF00741** 是 Gas vesicle protein family；课程材料中的 PF01132 对应 EF-P OB domain。PF00741 只能证明家族身份，不能同时充当监督标签和独立验证证据。

## 当前问题定义

### 输入

- `gv.zip` / NCBI RefSeq 来源的天然 GvpA 候选序列；
- recognition JSON 中有 2078 条按完全一致序列合并后的代表序列；
- accession、organism、description、source file 等元数据。

2078 是候选集规模，不等于质量控制后的最终分析规模。`partial`、非标准残基、GvpA/GvpJ 类型冲突和来源缺失均需保留标记并做敏感性分析，不能未经审计直接删掉后仍称为“clean 2078”。

### 标签可行性结论

历史数据库调查未发现可用的天然序列功能差异标签。当前可追溯的 M2 [PF00741 扫描摘要](results/bioinformatics/pf00741_scan_summary.json)记录：2076 条通过序列 QC 的输入全部 accepted（PF00741.24，GA 阈值）。这不是对全部 2078 条候选的功能分类；原始输出与运行环境仍待独立复核。

因此当前数据没有可用于天然序列功能分类的可靠 `y`：

```text
取消主线：sequence -> fabricated function label -> classifier
采用主线：sequence/window -> representation -> candidate region -> independent evidence
```

实验突变记录只用于候选区域冻结后的外部核对，不进入训练、聚类或候选筛选。任何 `Vac+`/`Vac-` 条目必须先完成亲本序列、位点编号、宿主、实验条件和来源表格的逐条映射。

## 研究目标

1. 建立可追溯、带质量标记的天然 GvpA 数据集。
2. 用 ESM-2 残基表示和滑动窗口寻找或复核局部序列模式。
3. 在同源分组的独立验证集上检验候选区域是否稳定。
4. 综合 PF00741 HMM/MSA 保守性、实验突变、已发表结构和残基扰动结果形成证据表。
5. 只报告“潜在功能区域/候选关键位点”，不声称完成湿实验功能证明。

## 研究问题

- **RQ1：区域稳定性。** 蛋白语言模型的局部表示是否能在同源分组的验证集上稳定定位候选区域，而不是只反映序列长度、物种或 C 端延伸？
- **RQ2：多源一致性。** 候选区域是否得到 HMM/MSA 保守性、已审计实验突变和 PDB 结构证据的共同支持？
- **RQ3：关键残基。** 哪些同源位点在残基扰动、保守性、突变和结构证据中持续获得高支持，且显著优于随机区域基线？

## 技术路线

```text
2078 条天然 GvpA 候选
          |
          v
元数据审计、完全重复核对、partial/冲突/异常残基标记
          |
          v
同源聚类 -> discovery / validation 分组
          |
          +----------------------+
          |                      |
          v                      v
ESM-2 残基表示              PF00741 HMM/MSA
30 aa 窗口、step 5           同源坐标与保守性
          |
          v
PCA/高维空间 HDBSCAN
UMAP 仅用于可视化
          |
          v
候选区域冻结并映射到 HMM 列
          |
          +------------+-------------+
          |            |             |
          v            v             v
残基遮挡/扰动       实验突变        PDB 7R1C
attention 补充      事后核对        结构映射
          |            |             |
          +------------+-------------+
                       |
                       v
            区域/残基多证据表与限制
```

### 1. 数据构建

- 上游数据已按完全一致序列合并；CD-HIT/MMseqs2 的主要用途是建立同源簇，不是再次声称得到 2078 条“去重后”数据。
- `partial` 和类型冲突先标记，主分析与敏感性分析分别报告。
- 非标准残基的排除必须保留 `exclusion_reason`。
- 数据划分按同源簇完成。无监督研究也需要 discovery/validation 隔离，避免在同一批样本上发现并验证候选区域。

### 2. 蛋白语言模型表示

当前开发结果采用 `facebook/esm2_t12_35M_UR50D`：每个残基为 **480 维**表示。若更换模型，必须按实际模型记录维度、层、池化方法和版本，不能统一写成 1024 维。

- 全长 mean pooling 用于检查整体异质性和混杂因素；
- 30 aa 窗口、步长 5 用于局部区域分析；
- 聚类在原始表示或 PCA 等保留较多信息的空间中进行；
- UMAP 只用于展示，不把二维图形本身当成功能证据。

### 3. 候选区域与坐标

窗口结果必须映射到同源坐标后比较。当前可用主坐标系是 PF00741 HMM match states；MAFFT 可作为敏感性分析。参考本科论文《基于生物大模型的气囊蛋白元件识别研究》已在同一类2078条GvpA数据上提出 **25–55** 和 **16–46**，因此本项目将二者登记为**预先冻结的假设**并进行独立验证，不能再次描述为本项目原创发现。方法与复用边界见 [论文审计](reports/M1/thesis_method_audit.md)。

### 4. 多源验证

- **保守性：** 在 PF00741 HMM/MSA 列上计算覆盖、熵和保守性，并报告 partial、类型冲突等敏感性。
- **突变：** 只使用能够映射到精确亲本序列和实验表型的记录，且在候选冻结后进行事后核对。
- **结构：** 优先使用已发表的 PDB 7R1C；ESMFold 仅作可选补充。当前开发记录显示 ESMFold API 调用失败，不能写成已完成证据。
- **模型证据：** attention 只称为 sequence-dependency evidence。关键位点至少增加遮挡、掩码或删除扰动，并与随机区域比较。

## Pfam 试扫描工具

仓库已实现分层选样、本地 InterProScan 包装、XML 解析和标签统计：

```bash
python -m unittest discover -s tests -p test_pilot_pfam.py -v
python scripts/pilot_pfam.py --help
```

入口为 `scripts/pilot_pfam.py`，规则见 `configs/pilot_pfam_rules.json`，操作说明见 [PILOT_PFAM.md](bioinformatics/PILOT_PFAM.md)。60 条试样的历史 `scan_not_run` 状态不代表 M2 全量扫描未运行；全量扫描及坐标结果已随 PR #13 进入 main，但不能替代独立复核。

## 五人分工

| 成员 | 主角色 | 下一阶段职责 | 互审对象 |
| --- | --- | --- | --- |
| A | 项目与证据统筹 | 冻结问题定义、发现/验证边界、实验矩阵、PR与报告 | 结论是否回答RQ |
| B | 数据工程 | 元数据审计、质量标记、同源聚类和发现/验证划分 | 与C互审序列和坐标 |
| C | 生物信息 | PF00741扫描、HMM/MSA、保守性、突变与7R1C映射 | D/E的生物学解释 |
| D | 表示学习 | ESM-2全长/窗口表示、聚类、稳健性和混杂检查 | 与E互审残基接口 |
| E | 残基证据与可视化 | 遮挡/扰动、attention补充、证据一致性、随机基线和图表 | D的模型结果 |

姓名与 GitHub 账号映射待成员确认。

## M1—M5 里程碑

| 阶段 | 核心交付 | 验收条件 |
| --- | --- | --- |
| M1 | 问题重定义、数据库标签不可用证据、RQ与验证设计 | 教师确认无监督区域路线或给出替代要求 |
| M2 | 数据卡、质量标记、同源簇、发现/验证划分、真实PF00741/HMM结果 | 输入、版本、哈希和排除原因可追溯 |
| M3 | ESM-2全长/窗口表示、聚类与参数稳健性 | 验证集复现，排除长度/物种等简单混杂 |
| M4 | 保守性、扰动、突变和7R1C多证据整合 | 候选冻结后验证，包含随机基线和敏感性 |
| M5 | 报告、PPT、结果冻结和非作者复现 | 所有数字绑定配置、数据版本和Git提交 |

## 仓库结构

```text
MLP/
├── configs/          # 固定实验参数
├── data/             # 数据定义与划分说明；受限原始数据不入Git
├── preprocessing/   # 审计、清洗、同源聚类与划分
├── bioinformatics/  # PF00741、HMM/MSA、保守性与结构映射
├── features/        # ESM-2表示
├── models/          # 无监督表示与聚类分析
├── attribution/     # 残基扰动和多证据一致性
├── experiments/     # 可复现实验入口与台账
├── results/         # 小型、可追溯结果和图表
├── reports/         # M1—M5报告与会议记录
└── tests/           # 数据、坐标和流程测试
```

## 实验记录规范

每次正式实验必须记录：数据版本、发现/验证划分版本、配置、随机种子、软件/数据库版本、完整 Git commit、输出哈希和限制。失败实验也进入 `experiments/registry.csv`。图表与统计量由脚本生成，不手工修改最终数字。

## 当前状态

- 已纠正 PF01132/PF00741 编号并完成标签可行性方向判断；
- 已从真实 `gv.zip` 冻结候选数据版本 `gvpa-recognition-c7f6f005d717`：2078条候选、2076条可进入同源聚类、1721条primary队列；
- 已用MMseqs2 15-6f452按80%一致性/80%覆盖率形成478个同源簇，并冻结 `homology-8b9005e2d9-s42`：discovery 1453条/335簇，validation 623条/143簇，同簇泄漏为0；
- PR #13/#14 已归档 B/C/D 技术结果和 E 图表；C 扫描与坐标技术成果已合并，独立复核未完成；
- 2026-09-26 助手技术预检：34 项测试和 release validator 通过，实际 split 无跨集合 ID/簇泄漏；这不是组员独立审查；
- 图表抽查发现 `cohort_counts.png` 四个非 primary 类误显示为 0；E 运行提交未知，PF00741 原始输出和共享访问尚未验证；
- **M2 待验收，M3 尚未正式启动。** 关闭门槛见 [验收报告](reports/M2/m2_acceptance.md)，复核任务见 [Issue #15](https://github.com/Lithium9767/MLP/issues/15)；
- 突变表逐条审计、残基扰动及 M3 表示学习仍属于后续任务，历史开发结果不作为最终生物学结论。

## 主要风险

1. **循环验证：** 同一批序列或同一Pfam证据不能同时用于发现和独立验证。
2. **坐标错位：** 原始序列位置、窗口位置、HMM列、文献编号和PDB编号必须显式映射。
3. **混杂因素：** 聚类可能主要反映长度、C端延伸、物种或partial状态。
4. **attention误读：** attention不是残基功能的因果归因。
5. **突变证据异质：** 不同宿主、构建体和实验条件不能简单合并为统一标签。
6. **结果先于复现：** 已生成图表仍需绑定提交、输入哈希和独立复跑记录。

## 下一步

- [ ] A向教师确认“无监督功能区域识别”是否满足课程目标。
- [x] B冻结候选数据清单、质量字段和输入/输出哈希。
- [x] B用MMseqs2生成同源簇并冻结discovery/validation划分。
- [x] C的PF00741扫描与HMM/序列/结构坐标技术成果已随PR #13归档。
- [ ] 未参与相关改动的B/C完成合并后独立复核，A完成最终验收（Issue #15）。
- [ ] E修正队列图、按干净提交重跑并补齐运行证据；C补充原始输出共享访问。
- [ ] D在验证集复现全长与窗口表示结果并完成参数敏感性。
- [ ] E实现至少一种残基遮挡/扰动方法及随机区域基线。
- [ ] A审查M2收尾与计划PR；M3仅准备ESM-2最小迁移草稿。B/C旧分支保留，不整体合并。
